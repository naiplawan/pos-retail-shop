# ⌨️ คู่มือคีย์ลัด - POS Retail Shop

## 📋 สารบัญ

- [คีย์ลัดหลัก (Essential)](#คีย์ลัดหลัก-essential)
- [การนำทาง (Navigation)](#การนำทาง-navigation)
- [การจัดการข้อมูล (Data Management)](#การจัดการข้อมูล-data-management)
- [ระบบพิมพ์และรายงาน (Reporting)](#ระบบพิมพ์และรายงาน-reporting)
- [ฟีเจอร์ขั้นสูง (Advanced Features)](#ฟีเจอร์ขั้นสูง-advanced-features)
- [คีย์ลัดเบราว์เซอร์ (Browser)](#คีย์ลัดเบราว์เซอร์-browser)
- [เคล็ดลับการใช้งาน](#เคล็ดลับการใช้งาน)

---

## 🎯 คีย์ลัดหลัก (Essential)

### 🔥 ใช้บ่อยที่สุด

| คีย์ลัด | ฟังก์ชัน | รายละเอียด |
|---------|----------|------------|
| `Ctrl + N` | **เพิ่มสินค้าใหม่** | เปิดฟอร์มเพิ่มสินค้าใหม่ทันที |
| `Ctrl + F` | **ค้นหาสินค้า** | เปิดหน้าต่างค้นหาขั้นสูง |
| `Ctrl + S` | **บันทึก/ดูยอดขาย** | บันทึกข้อมูลหรือแสดงสถิติการขาย |
| `Ctrl + P` | **พิมพ์รายงาน** | เปิดระบบพิมพ์รายงาน |
| `?` | **ความช่วยเหลือ** | แสดงคีย์ลัดทั้งหมดในป๊อปอัพ |

### 📱 สำหรับผู้ใช้มือใหม่

| คีย์ลัด | ฟังก์ชัน | เมื่อไหร่ใช้ |
|---------|----------|-------------|
| `Escape` | **ยกเลิก/ปิด** | ปิดหน้าต่างป๊อปอัพหรือยกเลิกการแก้ไข |
| `Enter` | **ยืนยัน** | กดแทนปุ่ม "ตกลง" หรือ "บันทึก" |
| `Tab` | **ย้ายช่องถัดไป** | เลื่อนไปช่องถัดไปในฟอร์ม |
| `Shift + Tab` | **ย้ายช่องก่อนหน้า** | เลื่อนกลับไปช่องก่อนหน้า |

---

## 🧭 การนำทาง (Navigation)

### หน้าหลัก
| คีย์ลัด | ปลายทาง | รายละเอียด |
|---------|---------|------------|
| `Ctrl + H` | **หน้าแรก (Home)** | กลับสู่แดชบอร์ดหลัก |
| `Ctrl + L` | **รายการ (List)** | ไปหน้ารายการคำสั่งซื้อ |
| `Ctrl + I` | **คลัง (Inventory)** | ไปหน้าจัดการคลังสินค้า |
| `Ctrl + R` | **รีเฟรช** | โหลดข้อมูลใหม่ |

### แท็บภายในหน้า
| คีย์ลัด | ฟังก์ชัน | ใช้ได้ที่ |
|---------|----------|----------|
| `1` | **แท็บแรก** | หน้า Dashboard |
| `2` | **แท็บที่สอง** | หน้า Dashboard |
| `3` | **แท็บที่สาม** | หน้า Dashboard |
| `4` | **แท็บที่สี่** | หน้า Dashboard |

### การเลื่อนหน้า
| คีย์ลัด | ฟังก์ชัน | รายละเอียด |
|---------|----------|------------|
| `Page Up` | **เลื่อนขึ้น** | เลื่อนหน้าจอขึ้นหนึ่งหน้า |
| `Page Down` | **เลื่อนลง** | เลื่อนหน้าจอลงหนึ่งหน้า |
| `Home` | **ไปจุดเริ่มต้น** | เลื่อนไปบนสุดของหน้า |
| `End` | **ไปจุดสิ้นสุด** | เลื่อนไปล่างสุดของหน้า |

---

## 📊 การจัดการข้อมูล (Data Management)

### การเพิ่มและแก้ไข
| คีย์ลัด | ฟังก์ชัน | ใช้ได้ที่ |
|---------|----------|----------|
| `Ctrl + N` | **สร้างใหม่** | ทุกหน้าที่มีฟอร์มเพิ่มข้อมูล |
| `Ctrl + E` | **แก้ไข** | เมื่อเลือกรายการในตาราง |
| `Ctrl + D` | **ลบ** | เมื่อเลือกรายการ (จะมีการยืนยัน) |
| `Ctrl + S` | **บันทึก** | ในฟอร์มการเพิ่ม/แก้ไขข้อมูล |

### การค้นหาและกรอง
| คีย์ลัด | ฟังก์ชัน | รายละเอียด |
|---------|----------|------------|
| `Ctrl + F` | **เปิดค้นหา** | เปิดหน้าต่างค้นหาขั้นสูง |
| `Ctrl + G` | **ตัวกรอง** | เปิด/ปิดแผงตัวกรอง |
| `Ctrl + K` | **ค้นหาด่วน** | Quick search box |
| `F3` | **หาถัดไป** | ไปยังผลลัพธ์ถัดไป |
| `Shift + F3` | **หาก่อนหน้า** | ไปยังผลลัพธ์ก่อนหน้า |

### การเลือกและจัดการ
| คีย์ลัด | ฟังก์ชัน | ใช้ในตาราง |
|---------|----------|-------------|
| `Ctrl + A` | **เลือกทั้งหมด** | เลือกทุกรายการในตาราง |
| `Ctrl + Click` | **เลือกหลายรายการ** | เพิ่มรายการในการเลือก |
| `Shift + Click` | **เลือกช่วง** | เลือกตั้งแต่รายการแรกจนถึงรายการที่คลิก |
| `Delete` | **ลบรายการ** | ลบรายการที่เลือก (มีการยืนยัน) |

---

## 🖨️ ระบบพิมพ์และรายงาน (Reporting)

### การพิมพ์
| คีย์ลัด | ฟังก์ชัน | รายละเอียด |
|---------|----------|------------|
| `Ctrl + P` | **พิมพ์หลัก** | เปิดระบบพิมพ์รายงาน |
| `Ctrl + Shift + P` | **พิมพ์ด่วน** | พิมพ์ด้วยการตั้งค่าล่าสุด |
| `Alt + P` | **ดูตัวอย่าง** | เปิดตัวอย่างรายงานในหน้าต่างใหม่ |

### รูปแบบรายงาน
| คีย์ลัด | รายงาน | รายละเอียด |
|---------|---------|------------|
| `1` | **ใบเสร็จ** | รายงานแบบ Thermal Receipt |
| `2` | **รายงานรายวัน** | สรุปการขายประจำวัน |
| `3` | **รายงานการขาย** | วิเคราะห์การขายแบบละเอียด |
| `4` | **รายงานสต๊อก** | สถานะคลังสินค้า |
| `5` | **สรุปการเงิน** | รายงานกำไรขาดทุน |

### Export ข้อมูล
| คีย์ลัด | ฟังก์ชัน | รูปแบบไฟล์ |
|---------|----------|-------------|
| `Ctrl + X` | **Export Excel** | ไฟล์ .xlsx |
| `Ctrl + C` | **Copy ข้อมูล** | คัดลอกไปยัง Clipboard |
| `Ctrl + J` | **Export JSON** | ไฟล์ .json (สำหรับ developer) |

---

## 🚀 ฟีเจอร์ขั้นสูง (Advanced Features)

### คลังสินค้า
| คีย์ลัด | ฟังก์ชัน | รายละเอียด |
|---------|----------|------------|
| `Alt + S` | **แจ้งเตือนสต๊อก** | แสดงรายการสินค้าเหลือน้อย |
| `Alt + R` | **เติมสต๊อกด่วน** | เติมสต๊อกสินค้าที่เลือก |
| `Alt + I` | **Import ข้อมูล** | นำเข้าข้อมูลจาก CSV/Excel |
| `Alt + E` | **Export สต๊อก** | ส่งออกข้อมูลคลัง |

### การพิมพ์ขั้นสูง
| คีย์ลัด | ฟังก์ชัน | เมื่อไหร่ใช้ |
|---------|----------|-------------|
| `F9` | **Thermal Print** | พิมพ์ใบเสร็จความร้อน |
| `F10` | **A4 Print** | พิมพ์รายงาน A4 |
| `F11` | **PDF Export** | บันทึกเป็น PDF |
| `F12` | **Email Report** | ส่งรายงานทาง Email |

### Offline Mode
| คีย์ลัด | ฟังก์ชัน | สถานการณ์ |
|---------|----------|-------------|
| `Alt + O` | **สถานะออฟไลน์** | ตรวจสอบสถานะการเชื่อมต่อ |
| `Alt + Y` | **บังคับซิงค์** | ซิงค์ข้อมูลทันที |
| `Alt + C` | **ล้าง Cache** | ล้างข้อมูล offline |

---

## 🌐 คีย์ลัดเบราว์เซอร์ (Browser)

### การจัดการหน้าเว็บ
| คีย์ลัด | ฟังก์ชัน | รายละเอียด |
|---------|----------|------------|
| `F5` | **รีเฟรช** | โหลดหน้าเว็บใหม่ |
| `Ctrl + F5` | **รีเฟรชแบบแรง** | ล้าง cache และโหลดใหม่ |
| `Ctrl + R` | **รีเฟรชธรรมดา** | โหลดหน้าเว็บใหม่ |
| `Ctrl + Shift + R` | **รีเฟรชแรง** | ล้าง cache (Chrome/Firefox) |

### แท็บและหน้าต่าง
| คีย์ลัด | ฟังก์ชัน | รายละเอียด |
|---------|----------|------------|
| `Ctrl + T` | **แท็บใหม่** | เปิดแท็บใหม่ |
| `Ctrl + W` | **ปิดแท็บ** | ปิดแท็บปัจจุบัน |
| `Ctrl + Shift + T` | **เปิดแท็บที่ปิดล่าสุด** | กู้คืนแท็บ |
| `Ctrl + Tab` | **แท็บถัดไป** | เปลี่ยนไปแท็บถัดไป |
| `Ctrl + Shift + Tab` | **แท็บก่อนหน้า** | เปลี่ยนไปแท็บก่อนหน้า |

### การซูมและมุมมอง
| คีย์ลัด | ฟังก์ชัน | ใช้เมื่อ |
|---------|----------|---------|
| `Ctrl + +` | **ซูมเข้า** | ตัวอักษรเล็กเกินไป |
| `Ctrl + -` | **ซูมออก** | ต้องการดูภาพรวม |
| `Ctrl + 0` | **รีเซ็ตซูม** | กลับขนาดปกติ |
| `F11` | **เต็มจอ** | ใช้พื้นที่เต็มหน้าจอ |

---

## ⚙️ คีย์ลัดระบบ (System)

### Developer Tools
| คีย์ลัด | ฟังก์ชัน | ใช้สำหรับ |
|---------|----------|----------|
| `F12` | **Developer Tools** | Debug และตรวจสอบปัญหา |
| `Ctrl + Shift + I` | **Inspector** | ตรวจสอบ Element |
| `Ctrl + Shift + J` | **Console** | ดู JavaScript errors |
| `Ctrl + Shift + C` | **Element Picker** | เลือก element บนหน้า |

### การตั้งค่า
| คีย์ลัด | ฟังก์ชัน | รายละเอียด |
|---------|----------|------------|
| `Ctrl + ,` | **การตั้งค่า** | เปิดหน้าการตั้งค่าระบบ |
| `Alt + ,` | **การตั้งค่าผู้ใช้** | การตั้งค่าส่วนตัว |
| `Ctrl + Alt + ,` | **การตั้งค่าขั้นสูง** | การตั้งค่าสำหรับผู้ใช้ขั้นสูง |

---

## 📱 คีย์ลัดบนมือถือ (Mobile)

### Gesture Controls
| การกระทำ | ฟังก์ชัน | รายละเอียด |
|----------|----------|------------|
| **แตะ 2 ครั้ง** | ซูมเข้า/ออก | บนตารางหรือกราฟ |
| **ปัดซ้าย** | ไปหน้าถัดไป | ใน slide หรือ carousel |
| **ปัดขวา** | กลับหน้าก่อน | ใน slide หรือ carousel |
| **ปัดลง** | รีเฟรช | บนหน้าหลัก |
| **กด 2 นิ้วและขยาย** | ซูม | บนกราฟหรือตาราง |

### Touch Shortcuts
| ตำแหน่ง | การกระทำ | ผลลัพธ์ |
|----------|----------|---------|
| **มุมบนขวา** | แตะ 3 วินาที | เมนู admin |
| **มุมล่างซ้าย** | แตะ 2 ครั้ง | สถิติด่วน |
| **แตะมุมจอ 4 มุม** | ลำดับ | Developer mode |

---

## 💡 เคล็ดลับการใช้งาน

### สำหรับผู้เริ่มต้น
1. **เริ่มจากคีย์ลัด 5 ตัว**: `Ctrl+N`, `Ctrl+F`, `Ctrl+S`, `Ctrl+P`, `?`
2. **ฝึกทีละตัว**: ใช้ 1 คีย์ลัดต่อวัน จนชิน
3. **ใช้ปุ่ม `?`**: เมื่อลืมคีย์ลัด
4. **สังเกต tooltip**: จะแสดงคีย์ลัดเมื่อ hover ปุ่ม

### สำหรับผู้ใช้ขั้นสูง
1. **Combo keys**: ใช้หลายคีย์ลัดต่อเนื่อง
   ```
   Ctrl+F → พิมพ์คำค้นหา → Enter → Ctrl+E
   (ค้นหา → เลือกผลลัพธ์ → แก้ไข)
   ```

2. **Custom shortcuts**: ตั้งค่าคีย์ลัดเพิ่มใน browser
3. **Macro sequences**: สร้าง sequence ของ actions

### การแก้ปัญหาคีย์ลัด

#### คีย์ลัดไม่ทำงาน
1. **ตรวจสอบ focus**: คลิกที่พื้นที่ว่างก่อน
2. **ปิด caps lock**: อาจทำให้คีย์ลัดใช้ไม่ได้
3. **ตรวจสอบ browser**: บาง extension อาจบล็อคคีย์ลัด
4. **กด ESC**: เพื่อยกเลิกการทำงานปัจจุบัน

#### คีย์ลัดชนกับเบราว์เซอร์
- **Chrome**: `Ctrl+D` (bookmark) vs App shortcut
- **Firefox**: `Ctrl+K` (search) vs App shortcut  
- **Safari**: `Cmd` แทน `Ctrl` บน macOS

### การปรับแต่งคีย์ลัด

#### Windows
1. เปิด **Sticky Keys** สำหรับผู้พิการ
2. ใช้ **AutoHotkey** สร้างคีย์ลัดเพิ่ม
3. การตั้งค่าใน **Windows Settings** > **Ease of Access**

#### macOS
1. **System Preferences** > **Keyboard** > **Shortcuts**
2. ใช้ `Cmd` แทน `Ctrl`
3. **Accessibility** options สำหรับ sticky keys

#### Linux
1. **Keyboard shortcuts** ใน system settings
2. ใช้ `xmodmap` สำหรับการปรับแต่งขั้นสูง

---

## 🎖️ Master Shortcuts (สำหรับมืออาชีพ)

### Workflow ที่เร็วที่สุด
```
เพิ่มสินค้าใหม่:
Ctrl+N → Tab → พิมพ์ชื่อ → Tab → พิมพ์ราคา → Tab → เลือกหมวดหมู่ → Enter

ค้นหาและแก้ไข:
Ctrl+F → พิมพ์ชื่อ → Enter → Ctrl+E → แก้ไข → Ctrl+S

พิมพ์รายงานด่วน:
Ctrl+P → 2 → เลือกวันที่ → Enter → ดูตัวอย่าง → Enter
```

### การจัดการข้อมูลจำนวนมาก
```
Import/Export:
Alt+I → เลือกไฟล์ → Enter
Ctrl+A → Ctrl+X → เลือกรูปแบบ → Enter
```

### Troubleshooting Shortcuts
```
เมื่อมีปัญหา:
F12 → Console → ดู errors
Ctrl+Shift+R → รีเฟรชแรง
Alt+O → ตรวจสอบสถานะ offline
```

---

## 📊 สรุปคีย์ลัดตามการใช้งาน

### ⭐ สำหรับงานประจำวัน (Daily Tasks)
| คีย์ลัด | ฟังก์ชัน | ความถี่การใช้ |
|---------|----------|---------------|
| `Ctrl + N` | เพิ่มสินค้าใหม่ | มาก |
| `Ctrl + F` | ค้นหาสินค้า | มาก |
| `Ctrl + P` | พิมพ์รายงาน | ปานกลาง |
| `F5` | รีเฟรชข้อมูล | ปานกลาง |
| `?` | ช่วยเหลือ | น้อย |

### 📈 สำหรับการวิเคราะห์ (Analytics)
| คีย์ลัด | ฟังก์ชัน | เหมาะสำหรับ |
|---------|----------|-------------|
| `Ctrl + S` | ดูยอดขาย | ผู้จัดการ |
| `2` | รายงานรายวัน | เจ้าของร้าน |
| `3` | รายงานการขาย | นักวิเคราะห์ |
| `Alt + S` | แจ้งเตือนสต๊อก | พนักงานคลัง |

### 🔧 สำหรับผู้ดูแลระบบ (Admin)
| คีย์ลัด | ฟังก์ชัน | สิทธิ์ |
|---------|----------|-------|
| `Ctrl + Alt + ,` | การตั้งค่าขั้นสูง | Admin |
| `F12` | Developer Tools | Admin |
| `Alt + I` | Import ข้อมูล | Admin |
| `Alt + C` | ล้าง Cache | Admin |

---

<div align="center">

## 🎯 เป็นมืออาชีพด้วยคีย์ลัด!

**การใช้คีย์ลัดจะเพิ่มประสิทธิภาพการทำงานได้มากกว่า 40%**

### 📖 อย่าลืม!
**กดปุ่ม `?` ได้ทุกเมื่อ เพื่อดูคีย์ลัดทั้งหมด**

📧 มีคำถาม? ติดต่อ support@yourstore.com

⌨️ **Happy Typing!** 

</div>