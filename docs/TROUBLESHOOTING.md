# 🛠️ คู่มือแก้ไขปัญหา - POS Retail Shop

## 🎯 **Build Status Update - All Fixed! ✅**

### ✅ **Recent Fixes (v2.0.1)**
- **Build Errors** - All TypeScript and ESLint errors resolved
- **Client Components** - Server/client boundary properly configured  
- **Dependencies** - All @radix-ui packages installed
- **Bundle** - Optimized to 927KB first load
- **Static Generation** - All 11 pages successfully generated

### 🔧 **Current Build Status**
```bash
✅ pnpm lint      # Passes with warnings (configurable)
✅ pnpm build     # Successful production build  
✅ pnpm dev       # Development server working
```

---

## 📋 สารบัญ

- [ปัญหาการติดตั้ง](#ปัญหาการติดตั้ง)
- [ปัญหาการใช้งาน](#ปัญหาการใช้งาน)
- [ปัญหาฐานข้อมูล](#ปัญหาฐานข้อมูล)
- [ปัญหา Electron Desktop App](#ปัญหา-electron-desktop-app)
- [ปัญหาการพิมพ์](#ปัญหาการพิมพ์)
- [ปัญหา Performance](#ปัญหา-performance)
- [ปัญหา Network/Offline](#ปัญหา-networkoffline)
- [ข้อผิดพลาดทั่วไป](#ข้อผิดพลาดทั่วไป)
- [การติดต่อขอความช่วยเหลือ](#การติดต่อขอความช่วยเหลือ)

---

## 🚀 ปัญหาการติดตั้ง

### ❌ Node.js Version ไม่ตรง

**ปัญหา**: Error เกี่ยวกับ Node.js version
```
Error: Node.js version mismatch. Required: >=18.0.0
```

**วิธีแก้**:
1. ตรวจสอบ Node.js version
   ```bash
   node --version
   ```
2. อัพเดท Node.js เป็น version 18+
3. หรือใช้ nvm เพื่อจัดการ version
   ```bash
   nvm install 18
   nvm use 18
   ```

### ❌ pnpm ไม่พบ

**ปัญหา**: `pnpm: command not found`

**วิธีแก้**:
```bash
# ติดตั้ง pnpm
npm install -g pnpm

# หรือใช้ npm แทน
npm install
npm run dev
```

### ❌ Dependencies ติดตั้งไม่ได้

**ปัญหา**: Error ระหว่างการติดตั้ง packages

**วิธีแก้**:
1. ล้าง cache และ node_modules
   ```bash
   rm -rf node_modules
   rm -rf .next
   pnpm store prune
   pnpm install
   ```

2. ใช้ --force flag (ถ้าจำเป็น)
   ```bash
   pnpm install --force
   ```

### ❌ Environment Variables หาไม่เจอ

**ปัญหา**: ไฟล์ `.env.local` ไม่มี หรือ config ผิด

**วิธีแก้**:
1. สร้างไฟล์ `.env.local` จาก template
   ```bash
   cp .env.example .env.local
   ```

2. แก้ไขข้อมูลให้ถูกต้อง
   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

3. Restart development server
   ```bash
   pnpm dev
   ```

---

## 🐛 ปัญหาการใช้งาน

### ❌ หน้าเว็บโหลดช้า

**อาการ**: หน้าเว็บใช้เวลาโหลดนานกว่า 10 วินาที

**สาเหตุที่เป็นไปได้**:
- อินเทอร์เน็ตช้า
- ข้อมูลในฐานข้อมูลมากเกินไป
- Browser cache เต็ม

**วิธีแก้**:
1. ตรวจสอบความเร็วอินเทอร์เน็ต
2. ล้าง Browser cache
   ```
   Chrome: Ctrl+Shift+Delete
   Firefox: Ctrl+Shift+Delete
   ```
3. ปิดแท็บอื่นที่ไม่ใช้
4. รีเฟรชหน้าเว็บ (F5)

### ❌ คีย์ลัดไม่ทำงาน

**อาการ**: การกด Ctrl+F, Ctrl+N ไม่มีผลอะไร

**สาเหตุ**:
- เคอร์เซอร์อยู่ในช่องพิมพ์ (input field)
- เบราว์เซอร์ไม่ support keyboard events

**วิธีแก้**:
1. คลิกที่พื้นที่ว่างก่อน
2. กด ESC เพื่อยกเลิกการพิมพ์
3. ลองใหม่อีกครั้ง
4. ถ้ายังไม่ได้ ให้ใช้ปุ่มบนหน้าจอแทน

### ❌ ข้อมูลไม่แสดงหรือแสดงผิด

**อาการ**: สถิติไม่ถูกต้อง หรือตารางว่างเปล่า

**สาเหตุ**:
- ยังไม่มีข้อมูลในระบบ
- Filter หรือ date range ไม่ถูกต้อง
- ปัญหาการเชื่อมต่อฐานข้อมูล

**วิธีแก้**:
1. ตรวจสอบว่ามีข้อมูลในระบบแล้ว
2. ลองเปลี่ยน date range หรือ filter
3. รีเฟรชหน้าเว็บ
4. ตรวจสอบ Network tab ใน Developer Tools

### ❌ การเพิ่มข้อมูลไม่สำเร็จ

**อาการ**: กดปุ่ม "บันทึก" แต่ข้อมูลไม่เพิ่มเข้าไป

**วิธีแก้**:
1. ตรวจสอบว่ากรอกข้อมูลครบทุกช่องที่จำเป็น
2. ตรวจสอบรูปแบบข้อมูล (ราคาต้องเป็นตัวเลข)
3. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
4. ดู Error message ใน browser console

---

## 🗄️ ปัญหาฐานข้อมูล

### ❌ เชื่อมต่อ Supabase ไม่ได้

**ปัญหา**: Cannot connect to Supabase database

**วิธีแก้**:
1. ตรวจสอบ Environment Variables
   ```bash
   echo $NEXT_PUBLIC_SUPABASE_URL
   echo $NEXT_PUBLIC_SUPABASE_ANON_KEY
   ```

2. ตรวจสอบ Supabase project status
   - เข้า Supabase Dashboard
   - ดูว่า project active หรือไม่

3. ตรวจสอบ API keys
   - Generate API key ใหม่ถ้าจำเป็น
   - อัพเดทใน `.env.local`

### ❌ RLS Policies ขัดข้อง

**ปัญหา**: "Row Level Security policy violated"

**วิธีแก้**:
1. ตรวจสอบ RLS policies ใน Supabase
2. Disable RLS ชั่วคราวเพื่อ debug
3. สร้าง policy ใหม่ตามตัวอย่าง:
   ```sql
   -- Allow users to see own data
   CREATE POLICY "Users can view own data" ON prices
     FOR SELECT USING (auth.uid() = user_id);
   
   -- Allow users to insert own data
   CREATE POLICY "Users can insert own data" ON prices
     FOR INSERT WITH CHECK (auth.uid() = user_id);
   ```

### ❌ Migration ล้มเหลว

**ปัญหา**: Database schema ไม่ตรงกับ application

**วิธีแก้**:
1. รัน migration script อีกครั้ง
   ```bash
   pnpm db:migrate
   ```

2. หรือสร้างตารางด้วยตัวเองใน Supabase SQL Editor:
   ```sql
   -- Create prices table
   CREATE TABLE IF NOT EXISTS prices (
     id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
     product_name TEXT NOT NULL,
     price DECIMAL(10,2) NOT NULL,
     category TEXT,
     date TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
     created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
   );
   
   -- Enable RLS
   ALTER TABLE prices ENABLE ROW LEVEL SECURITY;
   ```

---

## 🖥️ ปัญหา Electron Desktop App

### ❌ Electron App เปิดไม่ได้

**ปัญหา**: Desktop app crash หรือเปิดไม่ขึ้น

**วิธีแก้**:
1. ตรวจสอบว่า Next.js server รันอยู่
   ```bash
   pnpm dev
   ```

2. รัน Electron ใน development mode
   ```bash
   pnpm electron:dev
   ```

3. ตรวจสอบ Electron logs
   ```bash
   # Windows
   %APPDATA%\pos-retail-shop\logs\
   
   # macOS
   ~/Library/Logs/pos-retail-shop/
   
   # Linux
   ~/.config/pos-retail-shop/logs/
   ```

### ❌ Local Database Error

**ปัญหา**: better-sqlite3 error

**วิธีแก้**:
1. Rebuild native modules
   ```bash
   pnpm rebuild
   ```

2. ติดตั้ง native dependencies
   ```bash
   # Windows (ต้องมี Visual Studio Build Tools)
   npm install --global windows-build-tools
   
   # macOS (ต้องมี Xcode Command Line Tools)
   xcode-select --install
   
   # Linux (ต้องมี build-essential)
   sudo apt-get install build-essential
   ```

### ❌ Auto-updater ไม่ทำงาน

**ปัญหา**: Desktop app ไม่อัพเดทอัตโนมัติ

**วิธีแก้**:
1. ตรวจสอบ internet connection
2. ตรวจสอบ update server
3. Manual download ใหม่จาก GitHub Releases

---

## 🖨️ ปัญหาการพิมพ์

### ❌ เครื่องพิมพ์ไม่เจอ

**อาการ**: กดพิมพ์แล้วไม่มีอะไรเกิดขึ้น

**วิธีแก้**:
1. ตรวจสอบเครื่องพิมพ์:
   - เปิดเครื่องพิมพ์
   - เชื่อมต่อ USB หรือ WiFi
   - มีกระดาษและหมึก

2. ตรวจสอบ Browser settings:
   - อนุญาตการพิมพ์จาก website
   - เลือกเครื่องพิมพ์ที่ถูกต้อง

3. ใช้ "ดาวน์โหลด" แทนการพิมพ์ตรง

### ❌ Thermal Printer ไม่ใช้งานได้

**ปัญหา**: ใบเสร็จ thermal พิมพ์ไม่ออก

**วิธีแก้**:
1. ตรวจสอบขนาดกระดาษ (80mm)
2. ตั้งค่า page size ใน print dialog
3. ใช้ generic text driver
4. ลองพิมพ์ test page จาก Windows

### ❌ รายงาน PDF เพี้ยน

**ปัญหา**: Layout รายงานไม่ถูกต้องเมื่อพิมพ์

**วิธีแก้**:
1. ใช้ "ดูตัวอย่าง" ก่อนพิมพ์เสมอ
2. เลือก paper size ที่ถูกต้อง (A4)
3. ตั้งค่า margin เป็น "ปกติ"
4. พิมพ์เป็น PDF ก่อน แล้วค่อยพิมพ์จาก PDF reader

---

## ⚡ ปัญหา Performance

### ❌ แอพช้า/ค้าง

**อาการ**: การตอบสนองช้า หรือหน้าจอค้าง

**วิธีแก้**:
1. ปิด browser tabs อื่นที่ไม่ใช้
2. Restart browser
3. ล้าง browser cache
4. ตรวจสอบ RAM usage:
   ```
   Task Manager (Windows): Ctrl+Shift+Esc
   Activity Monitor (macOS): Cmd+Space → Activity Monitor
   ```

### ❌ ข้อมูลใหญ่โหลดช้า

**ปัญหา**: ตารางที่มีข้อมูลเยอะโหลดช้า

**วิธีแก้**:
1. ใช้ pagination (แบ่งหน้า)
2. กรองข้อมูลด้วย date range
3. ใช้ search แทนการดูทั้งหมด
4. Export ข้อมูลเป็น Excel แทน

### ❌ Real-time Updates ช้า

**ปัญหา**: ข้อมูลอัพเดทไม่ทันเวลาจริง

**วิธีแก้**:
1. รีเฟรชหน้าเว็บ
2. ตรวจสอบ Supabase Realtime status
3. ใช้ manual refresh button

---

## 🌐 ปัญหา Network/Offline

### ❌ ออฟไลน์โหมดไม่ทำงาน

**ปัญหา**: ใช้งานไม่ได้เมื่อไม่มีอินเทอร์เน็ต

**วิธีแก้**:
1. ตรวจสอบว่าเปิด Service Worker
2. อนุญาต browser ให้เก็บข้อมูล offline
3. Clear storage และลองใหม่:
   ```
   F12 → Application → Storage → Clear storage
   ```

### ❌ Sync ข้อมูลผิดพลาด

**ปัญหา**: ข้อมูลไม่ sync เมื่อกลับมาออนไลน์

**วิธีแก้**:
1. คลิกปุ่ม "บังคับ Sync" (มุมล่างขวา)
2. รีเฟรชหน้าเว็บ
3. ตรวจสอบ browser console เพื่อหา error

### ❌ CORS Error

**ปัญหา**: "Access to fetch blocked by CORS policy"

**วิธีแก้**:
1. ตรวจสอบ domain ใน Supabase settings
2. เพิ่ม domain ใน allowed origins
3. ใช้ proper API endpoint

---

## ❗ ข้อผิดพลาดทั่วไป

### Error Code: 500 - Internal Server Error

**ความหมาย**: เซิร์ฟเวอร์มีปัญหา

**วิธีแก้**:
1. รอสักครู่แล้วลองใหม่
2. ตรวจสอบ server logs
3. รีสตาร์ทแอพพลิเคชัน

### Error Code: 404 - Not Found

**ความหมาย**: หน้าเว็บหรือ API endpoint ไม่เจอ

**วิธีแก้**:
1. ตรวจสอบ URL ว่าถูกต้อง
2. ตรวจสอบ API routes ใน `app/api/`
3. ล้าง .next cache:
   ```bash
   rm -rf .next
   pnpm dev
   ```

### Error Code: 401 - Unauthorized

**ความหมาย**: ไม่ได้ Login หรือ session หมดอายุ

**วิธีแก้**:
1. Login ใหม่
2. ตรวจสอบ JWT token
3. ตรวจสอบ Supabase auth settings

### TypeError: Cannot read property 'X' of undefined

**ความหมาย**: JavaScript error - ข้อมูลไม่มี

**วิธีแก้**:
1. รีเฟรชหน้าเว็บ
2. ตรวจสอบว่าข้อมูลโหลดเสร็จแล้ว
3. ตรวจสอบ network requests ใน Developer Tools

---

## 🔧 Debug Tools

### Browser Developer Tools

1. **เปิด Developer Tools**
   ```
   Chrome/Edge: F12 หรือ Ctrl+Shift+I
   Firefox: F12 หรือ Ctrl+Shift+I
   Safari: Cmd+Option+I
   ```

2. **แท็บที่สำคัญ**:
   - **Console**: ดู JavaScript errors
   - **Network**: ดู API calls
   - **Application**: ดู Local Storage, Session Storage
   - **Elements**: ตรวจสอบ HTML/CSS

### ตรวจสอบ API Calls

1. เปิด Network tab
2. กรอง by "Fetch/XHR"
3. ทำ action ที่มีปัญหา
4. ดู response codes และ response data

### ตรวจสอบ Local Storage

1. F12 → Application → Local Storage
2. ดูว่ามี offline data หรือไม่
3. ลบข้อมูลเก่าถ้าจำเป็น

---

## 📞 การติดต่อขอความช่วยเหลือ

### ข้อมูลที่ต้องเตรียมก่อนติดต่อ

1. **ระบบปฏิบัติการ**: Windows 10/11, macOS, Linux
2. **เบราว์เซอร์**: Chrome, Firefox, Safari, Edge + version
3. **ขนาดหน้าจอ**: Desktop, Tablet, Mobile
4. **ข้อผิดพลาดที่เกิดขึ้น**: Screenshot หรือ error message
5. **ขั้นตอนที่ทำก่อนเกิดปัญหา**: อธิบายละเอียด

### วิธีการติดต่อ

#### 📧 Email Support
**support@yourstore.com**
- Response time: 24-48 ชั่วโมง
- เหมาะสำหรับ: ปัญหาเทคนิคที่ซับซ้อน

#### 💬 Live Chat
**Discord: https://discord.gg/yourstore-dev**
- Response time: Real-time (เวลาทำการ)
- เหมาะสำหรับ: ปัญหาด่วน, คำถามเบื้องต้น

#### 📝 GitHub Issues
**https://github.com/yourusername/pos-retail-shop/issues**
- เหมาะสำหรับ: Bug reports, Feature requests
- ต้องมี GitHub account

#### 📱 LINE Official
**@yourstore**
- Response time: 1-6 ชั่วโมง
- เหมาะสำหรับ: คำถามทั่วไป (ภาษาไทย)

### Community Support

#### Stack Overflow
- Tag: `pos-retail-shop`
- เหมาะสำหรับ: คำถาม technical ที่ซับซ้อน

#### Reddit
- r/pos-retail-shop
- เหมาะสำหรับ: Discussion, Tips & Tricks

---

## 🛡️ การป้องกันปัญหา

### สำรองข้อมูลเป็นประจำ

1. **Export ข้อมูล**เป็น CSV/Excel ทุกสัปดาห์
2. **Screenshot**การตั้งค่าสำคัญ
3. **บันทึก**รายละเอียดการตั้งค่า environment

### อัพเดทระบบสม่ำเสมอ

1. ตรวจสอบ **GitHub Releases** เป็นประจำ
2. อ่าน **CHANGELOG.md** ก่อนอัพเดท
3. **ทดสอบ**ใน development environment ก่อน production

### Monitor สุขภาพระบบ

1. ตรวจสอบ **Supabase Dashboard** เป็นประจำ
2. Monitor **API usage** และ **quota**
3. ตรวจสอบ **disk space** และ **RAM usage**

---

<div align="center">

## 🎯 หวังว่าคู่มือนี้จะช่วยแก้ปัญหาได้

**ถ้ายังแก้ไม่ได้ อย่าลังเลที่จะติดต่อทีมสนับสนุน**

📧 support@yourstore.com | 💬 Discord | 📱 @yourstore

**เรายินดีช่วยเหลือเสมอ!** 

</div>